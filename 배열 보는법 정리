queries[k]는 k번째 쿼리라는뜻.

[s, e, k]를 담는 쿼리라면 그건 "크기"가 3인 배열임. 값은 queries[i][0], queries[i][1], queries[i][2] 형태로 꺼낼 수 있다.
처음엔 queries[][][]형태로 하나씩 지목해야하나 했는데, 2차원 배열이라서 [][][]형태는 안된다고 이해했다.

정답 배열도 크기가 3이어야하니 malloc은 queries_row 크기여야함. 쿼리의 행 개수.
[rows(행)][cols(열)]임.








문제 설명

정수 배열 arr와 2차원 정수 배열 queries이 주어집니다. queries의 원소는 각각 하나의 query를 나타내며, [s, e, k] 꼴입니다.
각 query마다 순서대로 s ≤ i ≤ e인 모든 i에 대해 k보다 크면서 가장 작은 arr[i]를 찾습니다.
각 쿼리의 순서에 맞게 답을 저장한 배열을 반환하는 solution 함수를 완성해 주세요.
단, 특정 쿼리의 답이 존재하지 않으면 -1을 저장합니다.




입출력 예
arr	                                               
[0, 1, 2, 4, 3]	

queries
[[0, 4, 2],[0, 3, 2],[0, 2, 2]]	                 

result
[3, 4, -1]



queries = [
    [0, 4, 2],  <- 이게 0번째 행 (첫 번째 줄)
    [0, 3, 2],  <- 이게 1번째 행 (두 번째 줄)
    [0, 2, 2]   <- 이게 2번째 행 (세 번째 줄)
]

결국 이렇게 가로 세로로만 존재하는 2차원 배열이 맞다는게 확인이 된다.



따라서 이 배열속 숫자 하나하나를 가리키려면 작성을 queries[1][0] 와 같이 세로 둘째줄 가로 첫번째 숫자를 가리킬 수가 있다.







이게 처음에 작성한 코드이다.



#include <stdbool.h>
#include <stdlib.h>

int* solution(int arr[], size_t arr_len, int** queries, size_t queries_rows, size_t queries_cols) {
    for(size_t k = 0; k < queries_rows; k++){
        int m = queries[k][][];
        int n = queries[][k][];
        int o = queries[][][k];





처음엔 이런식으로 할 생각이었음. 당연히 틀렸지만..
size_t queries_rows, size_t queries_cols 만 주어진것만 보더라도 "2차원이구나" 하고 생각해야했던것 같음.






        
        int temp1 = m;
        int temp2 = n;
        
        if(arr[k] > temp1 && arr[k] < temp2){}




대충 이런식으로 하나하나 정해서 넣으려 했었음.
        
        
    }
    int a = 0;
    int cmp_prep = 0;
    for(size_t i = 1; i < arr_len; i++){
        if(arr[i-1] > 2){cmp_prep[i-1] = arr[i-1] - 2; a = cmp[i] < cmp_prep[i-1] ? cmp[i]:cmp[i-1];}
        }
    int* answer = (int*)malloc(1);
    return answer;
}



그후에 "하나씩 비교해서 넣으면 되려나?" 이런 생각 해보다가 결국 생각이 꼬여서 제미나이 도움받아버림...







아래가 옳게된 답안임.

#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

아래에서 이용하는 각각을 설명하자면

arr_len: arr 배열의 길이
queries_rows: 쿼리의 개수 (답 배열의 크기)
queries_cols: 각 쿼리의 원소 수 (언제나 3개: s, e, k), 즉 0, 1, 2.

이다.


int* solution(int arr[], size_t arr_len, int** queries, size_t queries_rows, size_t queries_cols) {
    


처음엔 결과를 담을 정답 배열을 쿼리 개수만큼 메모리 할당합니다. malloc을 이용함.
크기는 queries_rows로 null없이 할당한다.


    int* answer = (int*)malloc(sizeof(int) * queries_rows);
    
i를 0부터 시작시켜서 쿼리 하나씩 꺼내도록 한다.


    for (size_t i = 0; i < queries_rows; i++) {
        int s = queries[i][0]; 
        int e = queries[i][1];
        int k = queries[i][2]; 
        


k보다 크면서 가장 작은 값을 저장할 변수를 설정해야한다.
처음에는 아주 큰 값(또는 문제 제한사항인 1,000,000보다 큰 값)으로 초기화

대충 음식점으로 비유하자면 어느 국밥집에 들어가든 국밥 가격이 1000001원보다는 저렴할테니 가장 싼 가격의 국밥을 발견하자마자 이 1000001원을 갱신할 수 있도록 큰 수로 잡고 시작하는것임.
        


        int min_val = 1000001; 
        
s부터 e까지 arr[i]로 탐색


        for (int j = s; j <= e; j++) {
            // 조건: arr[j]가 k보다 커야 하고, 현재까지 찾은 최솟값(min_val)보다도 작아야함.
            if (arr[j] > k && arr[j] < min_val) {
                min_val = arr[j]; // 진짜 최솟값이 나올때까지 최솟값 갱신이 계속 될것임.
            }
        }
        
        // 만약 min_val이 초기값 그대로라면 k보다 큰 값을 못 찾은 것이므로 -1을 저장합니다.
        if (min_val == 1000001) {
            answer[i] = -1;
        } else {
            answer[i] = min_val;
        }
    }
    
    return answer;
}


완.

