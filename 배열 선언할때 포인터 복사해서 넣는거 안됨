#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int solution(int n, const char* control) {
    int answer = 0;
    
    int cmp = 0;
    
    char cmp_w_asd[] = control; <<< 이거 틀린거임!
    cmp = atoi(cmp_w_asd);
    answer = cmp > 84 ? n + 1 : n ;
    
    if(answer == n && cmp > 82){answer = n - 1;}
    else if(answer == n){answer = cmp > 66 ? n + 10 : n - 10;}
    
    return answer;
}

근데 이 문제는 이렇게 푸는게 아니었음.

문제 설명

정수 n과 문자열 control이 주어집니다. control은 "w", "a", "s", "d"의 4개의 문자로 이루어져 있으며, control의 앞에서부터 순서대로 문자에 따라 n의 값을 바꿉니다.
"w" : n이 1 커집니다.
"s" : n이 1 작아집니다.
"d" : n이 10 커집니다.
"a" : n이 10 작아집니다.
위 규칙에 따라 n을 바꿨을 때 가장 마지막에 나오는 n의 값을 return 하는 solution 함수를 완성해 주세요.
제한사항
-100,000 ≤ n ≤ 100,000
1 ≤ control의 길이 ≤ 100,000
control은 알파벳 소문자 "w", "a", "s", "d"로 이루어진 문자열입니다.
입출력 예
n	control	result
0	"wsdawsdassw"	-1
입출력 예 설명
입출력 예 #1
수 n은 control에 따라 다음과 같은 순서로 변하게 됩니다.
0 → 1 → 0 → 10 → 0 → 1 → 0 → 10 → 0 → -1 → -2 → -1
따라서 -1을 return 합니다.





이게 문제 내용이었는데, wasd로 한글자씩 주어지는게 아니라 wasd를 이용한 문자열이 주어지는 문제였던거임!!!


#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int solution(int n, const char* control) {
    int i = 0; while (control[i] != '\0') {
        switch (control[i]) {
            case 'w' : n += 1; break;
            case 's' : n -= 1; break;
            case 'd' : n += 10; break;
            case 'a' : n -= 10; break;
        }i++; 
        }return n;
}

이게 맞음 ㅇㅇ;;


